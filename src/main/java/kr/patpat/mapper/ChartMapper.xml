<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.patpat.mapper.ChartMapper">

	<select id="selectWeekly" parameterType="map" resultType="java.util.HashMap">
		select * from t_category a join t_action b on a.category_idx = b.category_idx where category_name not in ('일지')
		and b.mb_idx=#{mbIdx} and b.pet_idx=#{petIdx}
	</select>
</mapper>