<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.patpat.mapper.DiaryMapper">


	<select id="selectAction" resultType="java.util.HashMap" parameterType="map">
		select * from t_action a inner join t_category b on a.category_idx=b.category_idx
		inner join t_alarm_content c on a.category_idx=c.category_idx
		where a.mb_idx=#{mbIdx} and a.pet_idx=#{petIdx} and a.action_at like concat('%', #{date}, '%')
	</select>

</mapper>